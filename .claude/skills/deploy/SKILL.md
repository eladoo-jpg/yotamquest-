# Deploy to Vercel Production

1. Run `npm run build` and confirm zero errors
2. If build succeeds, run `vercel --prod`
3. Report the production URL
4. If build fails, list the errors and stop — do not deploy
